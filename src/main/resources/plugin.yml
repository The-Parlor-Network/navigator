name: Navigator
version: '${project.version}'
main: dev.clxud.navigator.Plugin
api-version: 1.19
prefix: NV
load: STARTUP
authors: [ cloudwithax ]
depend: [PlaceholderAPI]
description: Navigate your servers with ease

commands:
  servers:
    description: Show the server selector GUI
    usage: /<command>

  goto:
    description: Go to a server
    usage: /<command> <server>